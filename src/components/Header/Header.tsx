import { FC } from "react";
import cn from './header.module.css'
const Header: FC = () => {
    return (
        <header className={cn.header}>
           
        </header>
    )
}

export default Header